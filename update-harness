#!/bin/sh
# Repo wrapper. Prefer the ~/.local/bin shim after the first apply.
ROOT=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
if command -v python3 >/dev/null 2>&1; then
  exec python3 "$ROOT/update.py" "$@"
fi
exec python "$ROOT/update.py" "$@"
